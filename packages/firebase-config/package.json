{
  "name": "@purim/firebase-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Firebase client SDK initialization consumed by all browser apps",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@purim/types": "workspace:*",
    "firebase": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
