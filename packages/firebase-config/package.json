{
  "name": "@purim/firebase-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Firebase app initialization for client and server environments",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@purim/types": "workspace:*"
  }
}
