{
  "name": "@purim/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared utility functions (formatting, validation, date helpers) used across apps",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@purim/types": "workspace:*"
  }
}
