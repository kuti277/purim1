name: Deploy

on:
  push:
    branches:
      - main

  # Allow manual trigger from the GitHub Actions UI
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false   # Never cancel an in-flight deploy

jobs:
  build:
    name: Build for production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all apps and functions
        run: pnpm build

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-production
          path: |
            apps/showcase/dist
            apps/dashboard/dist
            apps/ivr-admin/dist
            functions/lib
          retention-days: 1

  deploy-hosting:
    name: Deploy Hosting (all sites)
    runs-on: ubuntu-latest
    needs: build
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-production

      # ── TODO: Uncomment and configure once Firebase project is created ──────
      #
      # - name: Deploy all hosting targets to Firebase
      #   uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: ${{ secrets.GITHUB_TOKEN }}
      #     firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      #     projectId: ${{ secrets.FIREBASE_PROJECT_ID }}
      #     channelId: live
      #     targets: showcase,dashboard,ivr-admin
      #
      # ────────────────────────────────────────────────────────────────────────

      - name: Deploy placeholder
        run: echo "Hosting deploy step — wire up FIREBASE_SERVICE_ACCOUNT secret to activate."

  deploy-functions:
    name: Deploy Cloud Functions
    runs-on: ubuntu-latest
    needs: build
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-production

      # ── TODO: Uncomment and configure once Firebase project is created ──────
      #
      # - name: Install Firebase CLI
      #   run: npm install -g firebase-tools
      #
      # - name: Deploy Cloud Functions
      #   run: firebase deploy --only functions --project ${{ secrets.FIREBASE_PROJECT_ID }}
      #   env:
      #     FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      #
      # ────────────────────────────────────────────────────────────────────────

      - name: Deploy placeholder
        run: echo "Functions deploy step — wire up FIREBASE_SERVICE_ACCOUNT secret to activate."
